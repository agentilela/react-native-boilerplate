import LoginSelectMethod from 'containers/LoginFlow/LoginSelectMethod'

const getLoginScreens = (registerScreen: (screenId: string, generator: () => {}) => void) => {
  // ADD PROJECT NAME
  // registerScreen('{PROJECT NAME}.LoginSelectMethod', () => LoginSelectMethod)
}

export default getLoginScreens
